[toc]

# Python:实现本地文件检索并在网页上打开(not final)

**document support**

ysys

**date**

2021-06-27

**label**

python,everything



## 问题

​	最近随着文档汇总越来越多,很多文档可能知识点都是类似,如一部分的数据库安装流程,只是操作系统版本或者数据库版本几个问题,就重新写了几篇文档,中间的重复部分甚至都不需要重新写的,或者可以直接引用其他部分,减少一部分的阅读量,尤其是突出不同点上无法明显体现出来。

​	这里就想写的技术文档分为基础类,如Gp的数据库安装(4.x,5.x,6.x)中部分知识点都是重复的,是否可以合并成为一个,或者只是在5.x,6.x标注一下。主要是感觉是很多知识或者技术是继承和发展的,在一个gp数据库中就可以看出来,之前22端口封禁导致gp4.x,gp5.x安装困难,而gp6.x就可以跳过这一点,这也只是一个方面。

​	之前已经做了一版数据内容检索的功能，只不是是将知识内容直接存储到pg数据库中，但是要在页面上打开pg数据库，执行查询sql的时候我就有的不想了,后来渐渐喜欢使用everything这个软件，这里在文件直接检索文件内容，并且可以执行多个条件查询，非常好用，不过，这样对我来说，还是不够方便，我想在网页上搜素





## 重要点

​	检索,多个正则,文件,打开文件